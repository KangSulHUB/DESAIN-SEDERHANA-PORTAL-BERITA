import Link from "next/link";
import { deleteArticleAction } from "@/app/actions/articles";
import { getAllArticles } from "@/lib/articles";

export default async function AdminPage() { const articles = await getAllArticles(); return <><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="text-sm font-bold uppercase tracking-widest text-orange-600">Dashboard</p><h1 className="text-3xl font-black text-slate-950">Manajemen berita</h1><p className="mt-1 text-slate-600">{articles.length} artikel dalam sistem.</p></div><Link href="/admin/berita/baru" className="rounded-lg bg-orange-600 px-4 py-3 text-center text-sm font-bold text-white hover:bg-orange-700">+ Buat berita</Link></div><div className="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-4">Berita</th><th className="p-4">Status</th><th className="p-4">Diperbarui</th><th className="p-4 text-right">Aksi</th></tr></thead><tbody className="divide-y divide-slate-100">{articles.map((article) => { const deleteWithId = deleteArticleAction.bind(null, article.id); return <tr key={article.id}><td className="p-4"><p className="font-bold text-slate-900">{article.title}</p><p className="mt-1 text-xs text-slate-500">{article.category} · {article.author}</p></td><td className="p-4"><span className={`rounded-full px-2 py-1 text-xs font-bold ${article.status === "published" ? "bg-green-100 text-green-700" : "bg-amber-100 text-amber-700"}`}>{article.status === "published" ? "Terbit" : "Draf"}</span></td><td className="p-4 text-slate-500">{new Intl.DateTimeFormat("id-ID", { dateStyle: "medium" }).format(new Date(article.updatedAt))}</td><td className="p-4"><div className="flex justify-end gap-2"><Link href={`/admin/berita/${article.id}/edit`} className="rounded-md bg-blue-50 px-3 py-2 font-bold text-blue-700 hover:bg-blue-100">Edit</Link><form action={deleteWithId}><button className="rounded-md bg-red-50 px-3 py-2 font-bold text-red-700 hover:bg-red-100">Hapus</button></form></div></td></tr>; })}</tbody></table></div>{articles.length === 0 && <p className="p-8 text-center text-slate-500">Belum ada berita.</p>}</div></>; }
