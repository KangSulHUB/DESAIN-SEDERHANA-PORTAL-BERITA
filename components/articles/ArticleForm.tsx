import type { Article } from "@/types/article";
import { CATEGORIES } from "@/types/article";

type Props = { article?: Article; action: (formData: FormData) => Promise<void> };

const inputClass = "mt-1.5 w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm text-slate-950 outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-100";
const labelClass = "block text-sm font-semibold text-slate-700";

export function ArticleForm({ article, action }: Props) {
  return <form action={action} className="space-y-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid gap-5 md:grid-cols-2"><label className={labelClass}>Judul berita<input className={inputClass} name="title" required minLength={8} maxLength={160} defaultValue={article?.title} /></label><label className={labelClass}>Penulis<input className={inputClass} name="author" required minLength={2} maxLength={80} defaultValue={article?.author} /></label></div><label className={labelClass}>Deskripsi singkat<input className={inputClass} name="description" required minLength={20} maxLength={300} defaultValue={article?.description} /><span className="mt-1 block text-xs font-normal text-slate-500">Ringkasan untuk kartu artikel dan SEO.</span></label><div className="grid gap-5 md:grid-cols-2"><label className={labelClass}>Kategori<select className={inputClass} name="category" defaultValue={article?.category || CATEGORIES[0]}>{CATEGORIES.map((category) => <option key={category}>{category}</option>)}</select></label><label className={labelClass}>Status<select className={inputClass} name="status" defaultValue={article?.status || "published"}><option value="published">Terbit</option><option value="draft">Draf</option></select></label></div><fieldset className="rounded-xl border border-orange-100 bg-orange-50/40 p-4"><legend className="px-1 text-sm font-bold text-slate-800">Thumbnail artikel</legend><label className={labelClass}>URL gambar<input className={inputClass} name="thumbnailUrl" type="url" placeholder="https://..." defaultValue={article?.thumbnailUrl} /></label><label className={`${labelClass} mt-4`}>Alt text gambar<input className={inputClass} name="thumbnailAlt" placeholder="Deskripsikan isi gambar" defaultValue={article?.thumbnailAlt} /></label></fieldset><fieldset className="rounded-xl border border-blue-100 bg-blue-50/40 p-4"><legend className="px-1 text-sm font-bold text-slate-800">Sumber berita</legend><div className="grid gap-4 md:grid-cols-2"><label className={labelClass}>Nama sumber<input className={inputClass} name="sourceName" placeholder="Contoh: Kompas.com" defaultValue={article?.sourceName} /></label><label className={labelClass}>Link sumber<input className={inputClass} name="sourceUrl" type="url" placeholder="https://..." defaultValue={article?.sourceUrl} /></label></div></fieldset><fieldset className="rounded-xl border border-red-100 bg-red-50/40 p-4"><legend className="px-1 text-sm font-bold text-slate-800">Video</legend><label className={labelClass}>Link YouTube atau Vimeo<input className={inputClass} name="videoUrl" type="url" placeholder="https://www.youtube.com/watch?v=..." defaultValue={article?.videoUrl} /><span className="mt-1 block text-xs font-normal text-slate-500">Frontend mengubah link yang didukung menjadi pemutar iframe yang bisa langsung diputar.</span></label></fieldset><label className={labelClass}>Isi berita<textarea className={inputClass} name="content" required minLength={50} rows={12} defaultValue={article?.content} /></label><div className="flex justify-end"><button className="rounded-lg bg-orange-600 px-5 py-3 text-sm font-bold text-white transition hover:bg-orange-700" type="submit">{article ? "Simpan perubahan" : "Buat berita"}</button></div></form>;
}
